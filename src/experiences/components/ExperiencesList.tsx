import type { ReactNode } from "react";

export const ExperiencesList = (): ReactNode => {
  return (
    <div>
      <h2>Liste des expériences</h2>
      {/* Ici, vous pouvez mapper vos expériences et les afficher */}
    </div>
  );
};
